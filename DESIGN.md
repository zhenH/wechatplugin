# 多人格群聊平台 — 设计文档 v1

> 一个内部使用的平台：真人和多个"不同人格的 agent"在同一个群里聊天。
> 每个 agent 拥有独立的人格提示词、知识库、触发规则和发言参数。
> 渠道先做 Web 聊天室，微信（wechaty 插件）作为实验性渠道。
> 另设"上帝 agent"：投喂角色资料，自动铸造新 agent 的人格提示词与知识库。

---

## 1. 产品模型

```
群（Room，Web 或微信）
   │  任何一条消息
   ▼
消息网关 ──广播──▶ Agent A ──┐
        ──广播──▶ Agent B ──┼─ 各自独立决策：回 or 不回（触发规则 + 浓度 + 冷却）
        ──广播──▶ Agent C ──┘
                             │ 决定回复
                             ▼
                    随机延迟 2~8 秒错开，发回群里
```

- 无编排、无主管：每个 agent 是"带着人格 + 知识库的独立聊天者"。
- 同一群可同时启用多个 agent，agent 之间也能互相交流（互 @ 名字即强触发）。
- 每个 agent 的上下文与知识库完全独立，互不泄露。

## 2. 核心概念

| 概念 | 说明 |
|---|---|
| **Agent** | 人格 prompt + 别名 + 知识库 + 触发关键词 + 发言参数（话痨度/冷却/模型） |
| **Room** | 一个群；绑定若干启用的 agent；有渠道属性（web / wechat） |
| **Message** | 群内一条消息；广播给所有启用 agent；记录各 agent 的浓度分数 |
| **God Agent** | 管理员专用的"人格铸造器"，输入角色资料 → 输出结构化角色档案 |

## 3. 群聊发言算法 v1

对每条入群消息 `m`（发送者 `S`，真人或 agent），并行计算每个启用 agent `A`：

### 3.1 全局静默规则
- 维护 `连续非真人消息计数`：真人发言 → 归零；agent 发言 → +1。
- 计数 ≥ 3 → 所有 agent 的**概率接话**暂停，直到真人发言。
- **真人 @ 名字 = 真人发言**：计数归零，被 @ 的 agent 强触发必答，不受静默影响。
- agent 之间互 @ 也构成强触发（必答候选），但计入非真人计数。

### 3.2 触发判定
- **强触发**：`m` 提到 `A` 的名字 / 别名 / 触发关键词 → 必答候选（仍受冷却约束）。
- **概率接话**：未强触发时，按 3.4 的概率决定。

### 3.3 话题浓度 `c(A, m) ∈ [0,1]` — 混合计算
1. **embedding 粗筛**：`m` 的向量 vs `A` 的知识库条目向量（无知识库则 vs 人格摘要向量）取最大相似度 `sim`。
2. **LLM 校准**（仅边界情况，省约 60% 调用）：
   - `sim < 0.25` → `c = sim`（低浓度直接判定，不调 LLM）
   - `0.25 ≤ sim ≤ 0.75` → 调便宜模型打分："你是[人格一句话摘要]，这条消息与你人设/知识领域的相关度 0~10"，`c = score/10`
   - `sim > 0.75` → `c = max(sim, 0.75)`（高浓度直接判定）
3. 分数记录在 Message 上，管理端可复盘。

### 3.4 发言概率 / 冷却 / 延迟
```
P(A)      = clamp( base_freq(A) × (0.3 + 2.2×c) × suppress , 0.02, 0.98 )
cooldown(A) = clamp( base_cd(A) / (0.4 + c) , 5s, 180s )
delay(A)  = 均匀随机(2s, 8s) × (1 - 0.5×c)
suppress  = 本条消息已被浓度 c ≥ 0.7 的 agent 抢答 → 0.3，否则 1
```

- 浓度高 → 更主动（P 高）、冷却短、先开口（delay 小）。
- 平时大部分时间按各 agent 自己的 `base_freq`（人格话痨度）走。
- 冷却未过 → 不发言；强触发也受冷却约束。

### 3.5 上下文
- 每个 agent 维护**自己的滚动上下文**：该群最近 `N=20` 条消息，格式化为 `张三: 今天好累` / `毒舌A: 那你别上班了`。
- 知识库检索只针对"正要回复的那条消息"，不拿全群历史去检索。
- 人设 prompt + few-shot 例句在每条消息里完整携带，防止人格漂移。

### 3.6 参数默认值（全部可配置，管理端滑块）

| 参数 | 默认 | 含义 |
|---|---|---|
| `N` | 20 | 上下文窗口条数 |
| `base_freq` | 0.3 | 人格话痨度（0.05~0.6） |
| `base_cd` | 30s | 基础冷却 |
| 静默上限 | 3 | 连续非真人消息数 |
| 延迟范围 | 2~8s | 发言随机延迟 |
| 冷却下限 | 5s | 防刷屏 |
| `suppress` | 0.3 | 被高浓度抢答后的降频因子 |
| 浓度阈值 | 0.25 / 0.75 | embedding 粗筛边界 |

## 4. 上帝 agent（人格铸造系统）

### 4.1 输入（多源投料）
粘贴文本 / 上传文件（PDF、Word、txt）/ 网页链接（角色百科、设定页）/ 参考对话样本。

### 4.2 输出 — 结构化角色档案（JSON）

```json
{
  "name": "毒舌影评人·老王",
  "aliases": ["老王", "影评人"],
  "system_prompt": "人格设定、语气、口头禅、价值观、知识边界、禁忌",
  "few_shot_examples": ["3~8条对话样本，示范语气"],
  "knowledge_entries": [
    {"title": "人物经历", "content": "...", "source": "来源链接"}
  ],
  "trigger_keywords": ["电影", "吐槽"],
  "suggested_params": {"base_freq": 0.4, "base_cd_s": 30}
}
```

### 4.3 两条铁律
1. **风格与知识分离**：`few_shot_examples`（语气样本）进 **prompt**，`knowledge_entries`（事实知识）进 **知识库 RAG**。混在一起是角色机器人做不好的主因。
2. **防幻觉**：知识条目必须带 `source` 来源引用，管理端可核对。上帝 agent 是"总结员"不是"创造者"。

### 4.4 流程
投料 → 上帝 agent 生成档案 → **管理端人工审核/编辑** → 满意后"激活"到群 → 不满意可继续对话迭代（"再毒舌一点"）。

### 4.5 实现要点
- 上帝 agent 本身是系统内的一个 agent（管理员专用），有自己的知识库（铸造方法论 + 已有角色模板）。
- 上帝 agent 用强模型（Claude / GPT-4o 级），群聊人格 agent 用便宜模型，成本分离。

## 5. 系统架构

```
┌────────────────────────────────────────────┐
│ 管理端 Web（控制面）                         │
│  Agent CRUD / 角色档案审核 / 群与规则 / 日志 │
└──────────────────┬─────────────────────────┘
                   │ API
┌──────────────────▼─────────────────────────┐
│ 运行时 FastAPI（数据面）                     │
│  消息网关(广播) → 触发判定 → 浓度计算        │
│  (embedding粗筛 + LLM校准) → 概率/冷却/延迟   │
│  上下文滚动窗口 / 会话持久化 / 护栏          │
└──────────────────┬─────────────────────────┘
                   │ HTTP/WS
        ┌──────────┴──────────┐
        ▼                     ▼
   Web 聊天室           微信插件（独立进程）
  （开发调试/产品形态）  wechaty 桥接，群聊@、限频、重连
```

- **控制面与数据面分离**：运行时是无状态执行器，从管理端拉配置。
- **渠道抽象**：统一消息事件模型（room / sender / sender_type / content / ts）；微信插件是独立进程，被封/掉线不影响平台。

## 6. 技术栈

| 层 | 选型 |
|---|---|
| 后端 | Python 3.12 + FastAPI |
| 数据库 | PostgreSQL + pgvector（配置、消息、会话、知识库向量一体） |
| 缓存/队列 | Redis（冷却状态、消息队列、延迟调度） |
| LLM | OpenAI 兼容接口（可切换国产模型）；上帝 agent 用强模型 |
| 向量化 | embedding API（与 LLM 同源） |
| 前端 | 管理端 + 聊天室（先简单：Vue/React 单页或服务端模板） |
| 微信 | wechaty（独立 Node 进程，HTTP/WS 桥接） |

## 7. 数据模型

```
Agent { id, name, aliases[], system_prompt, few_shot[], trigger_keywords[],
        params{base_freq, base_cd}, model, created_by(god|manual) }
KnowledgeEntry { id, agent_id, title, content, source, embedding }
Room { id, channel(web|wechat), agent_ids[], rules{} }
Message { id, room_id, sender, sender_type(human|agent), content, ts,
          concentration_scores{agent_id: c} }
Context { room_id + agent_id → 最近 N 条滚动窗口（Redis） }
```

## 8. 落地阶段（每阶段有可演示成果）

- **阶段 0**：FastAPI 骨架 + 管理端（创建 agent、人格 prompt、别名、触发词、参数滑块）+ **Web 聊天室**，3 个示例人格 agent 纯 prompt（无 RAG）跑通"真人 + 多 agent 同群聊天"。
- **阶段 1**：RAG 接入（上传文档 → 分块 → 向量化 → 检索），每个 agent 独立知识库；会话/消息持久化。
- **阶段 2**：浓度混合计算（embedding 粗筛 + LLM 校准）+ 完整发言算法参数化（概率/冷却/延迟/抑制/静默）。
- **阶段 3**：上帝 agent 铸造系统 + 角色档案审核界面。
- **阶段 4**：微信插件（wechaty 独立进程）接群聊：@ 触发、限频、重连、日志。
- **阶段 5**：监控面板（浓度记录、发言日志、token 成本）、评估调优。

## 9. 风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| 微信封号/掉线 | 高 | 插件与平台解耦、小号、限频、自动重连 |
| 刷屏 / 回声死循环 | 中 | 静默规则(3)、冷却、抑制因子、延迟错开 |
| 人格漂移 | 中 | 每条消息完整携带 prompt + few-shot |
| RAG 检索质量 | 中 | 混合检索（向量 + 关键词）、评估集 |
| Prompt 注入 / 知识串库 | 中 | agent 上下文与知识库强隔离、输出过滤 |
| token 成本 | 中 | 上下文裁剪、embedding 粗筛省调用、模型分级 |
| 上帝 agent 幻觉 | 中 | 档案必须带 source、人工审核环节 |
